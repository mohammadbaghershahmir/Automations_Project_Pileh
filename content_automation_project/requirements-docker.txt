# Slim dependencies for Dockerfile.webapp (API + Celery worker, Stage V / Test Bank).
# Avoids PyMuPDF, GUI, sentence-transformers, and heavy PDF stacks — not needed for server-side Test Bank.
# For the full desktop app on your machine: pip install -r requirements.txt

requests>=2.31.0
python-dotenv>=1.0.0
python-docx>=1.1.0

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
sqlalchemy>=2.0.25
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# bcrypt 4.1+ drops __about__; passlib 1.7.x breaks on it (startup crashes).
bcrypt>=4.0.1,<4.1
redis>=5.0.1
celery[redis]>=5.3.0
jinja2>=3.1.3
jdatetime>=4.1.0
