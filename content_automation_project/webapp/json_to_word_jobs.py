"""Web job types that convert Document Processing JSON to Word (.docx)."""

from __future__ import annotations

JSON_TO_WORD_JOB_TYPES = frozenset({"document_processing_json_to_word"})

JSON_TO_WORD_JOB_LABELS = {
    "document_processing_json_to_word": "Document Processing → Word",
}
